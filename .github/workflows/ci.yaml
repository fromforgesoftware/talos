name: CI
on:
  push:
    branches: [main]
  pull_request:
jobs:
  go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: talos
      - uses: actions/checkout@v4
        with:
          repository: fromforgesoftware/go-kit
          path: go-kit
      - uses: actions/setup-go@v5
        with:
          go-version-file: talos/go.mod
      - name: build + vet + test (go-kit via workspace)
        run: |
          cd talos
          go work init . ../go-kit
          go build ./...
          go vet ./...
          go test ./...
